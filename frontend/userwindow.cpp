#include "userwindow.h"
/*#include "ui_userwindow.h"

UserWindow::UserWindow(QString ID, QWidget *parent) :
    QDialog(parent),
    ui(new Ui::UserWindow)
{
    ui->setupUi(this);
  ui->labeliduser->setText(ID);
   objectUserWindow = new UserWindow;


}

UserWindow::~UserWindow()
{
    delete ui;
}
*/
